#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    printf("Enter a number decimal \n");
    scanf("%d", &a);

   printf("The number in octal is %o \n", a);
   printf("The number in Hexadecimal is %x \n",a);

    return 0;
}
